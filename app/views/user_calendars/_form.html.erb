<%= form_with model: @user_calendar, autocomplete: :off, local: true do |form| %>

  <div class="form-group">
    <label>Data </label>
    <%= form.text_field :calendar_date, class: 'form-control', type: 'date', autofocus: true %>
  </div>

  <div class="mt-4">
    <div class="d-flex justify-content-between">
      <%= form.submit 'Zapisz', class: 'btn btn-sm btn-primary' %>
      <%= link_to 'Powrót', user_calendars_path, class: 'btn btn-sm btn-secondary' %>
    </div>
  </div>

<% end %>
